#include <stdio.h>
int main() {
    int n, i, j, isPrime;
    for(n = 2; n <= 500; n++) {
        isPrime = 1;
        for(i = 2; i <= n/2; i++) {
            if(n % i == 0) {
                isPrime = 0;
                break;
            }
        }
        if(isPrime)
            printf("%d ", n);
    }
    return 0;
}
