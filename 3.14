#include <stdio.h>
int main() {
    int i, n, sum = 0;
    float mean;
    printf("Enter 10 numbers:\n");
    for(i = 1; i <= 10; i++) {
        scanf("%d", &n);
        sum += n;
    }
    mean = sum / 10.0;
    printf("Sum = %d, Mean = %.2f\n", sum, mean);
    return 0;
}
